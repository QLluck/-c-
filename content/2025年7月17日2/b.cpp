#include <bits/stdc++.h>
using namespace std;
#define ll long long
const int N = 1e5 + 7;

int a[N]={0} ;
int b[N]={0} ;
int gcd(int a,int b)
{
     return  a?gcd
}
void solve()
{
    int n, k;
    cin>>n>>k;
    for (int i = 1; i <= n;i++)
        cin >> a[i];
    for (int i = 1; i <= n;i++)
        cin >> b[i];
    
}
int main()
{
    ios::sync_with_stdio(0), cout.tie(0), cin.tie(0);
    int t;
    cin >> t;
    while (t--)
        solve();
}